"use client"

import React from 'react';

export const metadata = {
    title: 'Account | Online Shop Next App',
    description: 'Get your account',
}

const Account = (props) => (
    <>account</>
);

export default Account;